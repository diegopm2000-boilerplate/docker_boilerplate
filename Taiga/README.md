# Dockerización de taiga

Manual obtenido de:

https://hub.docker.com/r/htdvisser/taiga-back/

# Arranque inicial

Para arrancar los contenedores inicialmente del docker-compose.yml hacemos:

```shell
$ docker-compose up
```
